(()=>{const header=document.querySelector('[data-site-header]');const toggle=document.querySelector('[data-menu-toggle]');const nav=document.querySelector('[data-primary-nav]');if(!header||!toggle||!nav)return;const mq=matchMedia('(max-width:900px)');const close=(focus=false)=>{header.classList.remove('is-open');toggle.setAttribute('aria-expanded','false');if(mq.matches)nav.setAttribute('inert','');else nav.removeAttribute('inert');if(focus)toggle.focus()};const open=()=>{nav.removeAttribute('inert');header.classList.add('is-open');toggle.setAttribute('aria-expanded','true');nav.querySelector('a,button')?.focus()};toggle.addEventListener('click',()=>header.classList.contains('is-open')?close(true):open());document.addEventListener('keydown',e=>{if(e.key==='Escape'&&header.classList.contains('is-open')){e.preventDefault();close(true)}});document.addEventListener('pointerdown',e=>{if(header.classList.contains('is-open')&&!header.contains(e.target))close()});mq.addEventListener?.('change',()=>close());close()})();